// src/hooks/useThemeColors.ts

/**
 * Provides the theme colors for light and dark modes.
 * In a real-world scenario, these could be fetched from a CSS file or a config.
 */
export const useThemeColors = () => {
  return {
    light: '#f5f5f0', // zayna-cream
    dark: '#1f2933',  // Dark charcoal
  };
};
